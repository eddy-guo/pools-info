# Pools Info implementation handoff

The user requested a settling point before account usage runs out. Finish the
validation/deployment of this iteration, then continue in another chat. Do not
start another architecture expansion merely because the persistent goal is active.
The original user-supplied review remains untouched in root `HANDOFF.md`.

## Latest deployed checkpoint

Implementation `a14e4cc` passed full CI34924987243 and is active on Railway indexer
deployment `5712e18d-ff29-4ca0-8a2f-0b2f704d5cd7`. Production logs confirmed
"Database migrations applied" at 03:33 UTC and "writer_acquired" at 03:34 UTC
on 2026-09-15. No new service, key or Railway setting was needed.

At 03:34:42 UTC all five health-script endpoints returned HTTP 200: 275 catalog
pools, 240 processed pools, 192 qualifying traders. The sampled wallet/leaderboard
PnL still matched exactly. Feed age was 4,780 seconds (46,846 blocks behind), so
freshness and full coverage remain incomplete. Remaining account usage was 2%.

## Launch-source database groundwork

Migration 007 and `commitBatch` now track multiple verified source observations
per pool, reject identity conflicts atomically, and preserve events/positions when
one source rewinds. The final source's removal still invalidates the pool and its
history. All 33 local Postgres checks passed, including upgrades from 001-006 and
both rewind orders. Read `docs/LAUNCH-SOURCES.md` before implementing the importer.
No candidate was imported or discovery cursor reset in this change. The next step
is the canonical candidate import/scheduling path using this source reconciliation.

## Resumable evidence collection

Latest implementation `be82a79` is pushed. Full GitHub validation 34924245870
passed, including application, Postgres, production container and browser checks.
Remaining account usage was 4% at the last check. This is a verified transfer point;
do not confuse finished evidence tooling with completed catalog coverage or import.

`scripts/verify-launch-candidates.ts --plan <1-5>` lists the next unsaved candidates
without RPC work. Use `--limit <1-5>` with root `.env.local` loaded to verify them
serially. A real one-candidate run succeeded in 42 RPC calls, leaving four saved
snapshots under ignored `.data/bootstrap`. Restart selection, lock exclusion and
failure cleanup were checked. See `docs/BOOTSTRAP-CANDIDATES.md` for commands and
limits. This is evidence collection only: safe database promotion remains the next
product-critical step, and saved snapshots must be canonically revalidated there.

## Additional bounded verification after the checkpoint

Two additional CLI runs verified real launches in 41 RPC calls each. Full evidence
is committed in `data/registry/candidate-proofs/` and described at the end of
`docs/BOOTSTRAP-CANDIDATES.md`. The cases include an already-present production pool
(CHARTY, block 62805658) and a missing historical pool (block 55314563). Use these
for overlap and new-import integration tests. No database imports, cursor changes,
provider configuration changes or implementation edits were made in this follow-up.

## Latest checkpoint

Implementation commit `39fa98e` is pushed to `main`. GitHub validation run
34923429841 passed in full, including browser tests and both production container
checks, for this candidate verifier iteration. Local lint,
typechecks, 128 unit tests and 30 isolated Postgres tests passed (six additional
unit cases skipped because they require the separate database suite).

At 2026-09-15 03:02 UTC the production read-only health check returned HTTP 200
for all five endpoints: 270 catalog pools, 237 processed pools, 192 qualifying
traders. One wallet's exact realized PnL matched its leaderboard entry. Recent
feed age was 3,876 seconds with a 38,157-block lag. This is healthy serving of
incomplete/stale data, not a fully live or complete product. No production candidate
import has been made. Preserve that distinction in the next chat.

## Start here when resuming

Verified web implementation `4807ca4` passed full CI34921297796 and was verified on
Vercel. Worker implementation `63fa0b4` also passed full CI and production batch
verification. Remaining quota was 5% at the last account check. Keep the existing
Railway/Postgres connections and provider defaults unless the user changes the
pending Alchemy plan decision. No new API key is currently required.

The latest discovery experiment found a cheaper bootstrap path for prominent missing
pools: read `docs/BOOTSTRAP-CANDIDATES.md` first. It preserves193 public candidate
hints (161 Instant,32 CCA) and a full on-chain PROLOGUE launch proof obtained in40
RPC calls. A reusable verifier and explicit one-candidate CLI now exist; a fresh
CLI run verified the same launch in 41 calls. Unit tests, all 30 Postgres tests and
typechecks passed. Evidence is saved atomically in ignored `.data/bootstrap`, with
no database mutation. Production does not contain that verified pool yet. Prioritize
safe provenance-aware promotion of the collected candidates; do not import
unverified hints or API financial metrics. Full discovery still needs versioned
historical rescanning. Inspection of current code confirms:

1. `ensureDiscovery` in `packages/db/src/index.ts` creates only `discovery:v1`
   and rejects changes to its saved start. `apps/indexer/src/main.ts` explicitly
   schedules that key. Adding verified strategy addresses does not revisit blocks
   already passed, nor the history before62625935. Earliest verified deployment
   is22754669. Preserve the existing cursor and introduce separately identifiable
   coverage for the pinned registry revision.
2. Migration 007 adds `pool_launch_sources`; matching observations preserve existing
   streams and conflicts roll back. Source deletion now chooses another observation
   or removes the pool only after its final source disappears. This supersedes the
   earlier single-source-cascade concern. See `docs/LAUNCH-SOURCES.md`.
3. Candidate import and versioned discovery scheduling remain unwired. Keep their
   ranges and canonical reconciliation explicit. The source table does not itself
   prove that a batch came from current chain evidence or that a scan is complete.

Paid RPC capacity would make that scan practical, but does not implement these
missing application behaviors. No rescan migration or historical cursor mutation
has been made. This section is an implementation handoff, not a claim of completion.

## Product and authorized scope

- Pools.xyz / Pools.trade launches on Robinhood chain 4663 only, not every ERC20
  or every Uniswap pool. Both historical and current supported deployments matter.
- Keep Explore, token/pool details, trader leaderboard, public wallet/creator
  profiles, Ctrl-K search, PnL cards, local watchlists/follows and account/copy-trade
  design. Do not delete routes because their data is incomplete.
- No account sign-in or persisted personal profiles yet. No fake financial data.
- Design source: `/Users/eddyguo/Downloads/Poolsinfo Design System/`.
- Direct commits/pushes to main authorized; unsigned project commits authorized.
  No PR required, no agent coauthor, no manual edits to generated files/CHANGELOG.
- Explain infrastructure in plain language. Surface needed manual actions early.

## Infrastructure

Repository: `/Users/eddyguo/Desktop/Work/Projects/pools-info`.
GitHub: `eddy-guo/pools-info`, main. Use `~/.local/bin/gh-axi` for GitHub operations.
Next.js on Vercel: https://www.poolsinfo.com/ . Namecheap domain already connected.
Railway API: https://api-production-9f93.up.railway.app .
Railway project `zestful-fulfillment`, ID `51279532-d7a4-491e-92db-e30997ed9223`.
Production environment `0464eb3f-148d-46a5-9b08-759576f682f1`.
Indexer service `d41596b3-6c74-4f96-a0fe-b6655c98ba95`.
API service `f837f83b-d423-4f00-8d10-61455466584b`.

Private Postgres uses Railway's default image/volume/private networking.
Worker/API reference `DATABASE_URL=${{Postgres.DATABASE_URL}}`. Do not expose the DB
or copy its URL into Vercel. API reads are read-only transactions, with no RPC.
One worker container supervises historical collection, analytics, and recent
collection as independently locked processes. Unexpected child exit drains and
restarts the unit. `RECENT_ENABLED=1` is enabled in production.

Existing Alchemy `ROBINHOOD_RPC_URL` is the only hosted chain provider. Public
Robinhood RPC returned HTTP 403 from Railway, despite working on the laptop.
Do not bypass that restriction. No Envio runtime integration exists.
ENS resolution is a separate Ethereum lookup, already present.
Secrets are in ignored root `.env.local` and Railway. Never print them.

Local dev: `pnpm dev`, port 3100. `scripts/web.mjs` loads root `.env.local` correctly
before starting Next. Do not replace it with `node --env-file ... next dev`;
Next's child process rejects propagated env-file flags.
Local tests: `TEST_DATABASE_URL=postgresql://eddyguo@127.0.0.1:55439/postgres`.
The local cluster is running; tests create isolated schemas. Never use production
DATABASE_URL for tests. `pnpm build` then `pnpm test:e2e` uses production port 3101.

## Changes in this iteration

1. Verified Instant registry expanded from 2 to 12 strategies across 6 generations,
   mapped to 3 launchers. Official Uniswap SDK pin:
   `2b210b8ef8eb7e7c041e9ca1d95a39b2e1f9dd6f`.
   Registry stores activation blocks, runtime hashes, tick spacing, creator fees,
   and fee splitter. Source/evidence: `docs/DEPLOYMENT-REGISTRY.md`, `data/registry/`.
   Real omitted historical POOLS launch at block 23467030 now validates, with 16
   swaps in a 10-block capture; current August-generation launch still validates.
   Later deployments are not required to exist at old capture cutoffs.
2. Migration 005 adds normalized publication markers, exact wallet positions,
   observed/audited trades with per-sale cost basis and closures, and prices.
   Atomic publication and FK rollback preserve evidence/reorg correctness.
   Existing snapshots backfill one pool per transaction without RPC at analytics
   startup. Explicit command from repo root:
   `node --import tsx apps/indexer/src/analytics-main.ts backfill`.
3. API Explore/search/leaderboard/wallet reads now use SQL projections and bounded
   responses. Pool detail loads only its own snapshot. Global reads no longer
   require loading all raw snapshots under a 500-publication cap. Full catalog
   filtering/sorting/pagination stays in SQL, including metadata-only launches.
   Wallet trade/position/launch lists and charts expose truncation/sampling;
   aggregate totals include the full saved eligible history.
4. Migration 006 adds pg_trgm and address/hash prefix indexes. GIN fastupdate is
   disabled for the four metadata indexes to avoid pending-list search latency.
5. Live ingestion resolves only pool IDs observed in its current bounded logs,
   rather than loading the entire registry and failing above 10,000 markets.
   Batch commits validate only referenced markets, with unchanged proof checks.
6. UI shows bounded-list/chart notices and fixes launch-card name/age spacing.
   Skeleton loading, stale-data retention, local follows and live-trade polling
   were already shipped in the preceding iteration.

## Correctness and rollout

Core accounting remains integer average-cost with carried basis. Window selection
filters sales; it never resets inventory at the beginning of a window. Excluded
positions retain null financials and reasons. Transaction initiator is not proof
of beneficiary. Recent-window swaps cannot establish complete PnL or holders.

Independent review caught/fixed SQL price-change rounding and a lost catalog
identity-consistency gate. Pure projection parity covers the committed real pool
corpus across every time window, including exclusions. Database tests cover
atomic replacement/rollback, reorg cascades, lock-skip/resume,501 publications,
large exact numbers and12,003 catalog search. UI remains production-contract tested.

Apply migrations 005/006 through the worker pre-deploy command, then let analytics
startup populate projections. New API readiness waits for required projection
schema/data; do not manufacture zero rankings while backfill is pending. The API
and worker deploy independently from main after CI. Confirm actual migration and
backfill logs, API readiness and real pages; a green build alone is insufficient.

## Remaining work, in order

- Historical global collection is NOT implemented by this iteration. Current
  historical collector still scans discovery plus round-robin per-pool histories.
  Replace market-wide historical work with shared block-range collection, keeping
  per-pool deep audits as targeted verification. Preserve transfer/birth evidence
  required for holders/cost basis; manager swaps alone are insufficient.
- Registry expansion is NOT retroactive coverage. Existing discovery:v1 started
  at 62625935 and old batches only recognized the old 2 strategies. Earliest newly
  verified deployment is 22754669. Need explicit versioned historical rescan with
  durable cursor and sound overlapping-launch provenance/idempotency. Do not reset
  cursors destructively or claim old ranges were rescanned.
- Recent collector is moving but was still stale on the latest pre-deploy check.
  Inspect `/v1/live-trades` coverage and recent_batch logs. A sample found current
  Pools.xyz launches still use the August pair, so missing registry entries are
  not the only cause of delayed activity. Unknown older pools also require launch
  backfill before their new trades can enter the verified feed.
- Crowd/LBP/CCA discovery and auction cost basis are not implemented. Do not add
  LBP addresses to the Instant decoder or invent synthetic buys without evidence.
- Holder refresh and financial projections only cover sufficiently verified
  histories. Complete market-wide coverage and near-head analytics remain goals,
  not delivered guarantees.
- Do not prune raw evidence on an arbitrary 7-14 day timer until rebuild, audit,
  reorg and provenance requirements have a tested retention design.

## Manual action pending

The earlier request to enable Alchemy Pay As You Go is still unconfirmed.
Current configured log range is 10 blocks, max 2 RPC calls per HTTP batch, minimum
1 second spacing per process. Three workers share the same account allowance.
If the user upgrades the existing account, keep the same key, verify a larger
eth_getLogs range, then tune bounded collection against the actual allowance.
Do not assume an upgrade or start a paid subscription. No other service/key is
currently required. HyperSync is a distinct authenticated API, not a JSON-RPC URL
that can be pasted into INDEXER_LOG_RPC_URL.

A single fee-splitter NFT balance was 52,404 at block 63241342; this is not a total
Pools count. The supplied review's 250k-350k estimate and 16-hour backfill estimate
are unverified. Measure actual activity and provider throughput before estimates.

The persistent goal remains incomplete. The user should pause it when handing
off, so the old chat does not automatically begin another iteration.

## Validation checkpoint

Local build, lint and all workspace typechecks passed. All 54 desktop/mobile
browser tests passed with zero retries. The root database suite passed 26/26;
subsequent focused API tests also verify GIN query plans and identity-conflict
readiness. Chain suite: 42/42. CI and production rollout should be checked for
the commit containing this handoff before declaring the deployment complete.

## Verified rollout after the checkpoint

Implementation commit: `0f7b3a94733e76f0e8ef53864c23acdb512d068b`.
GitHub CI run `34915453189` passed all stages, including Postgres, both Docker
images, migration/API container checks and browser tests. Vercel reports successful
deployment of that implementation commit.

Railway indexer deployment `a9e36deb-51c9-4862-8583-c0aad1ffe85f` became Active.
Observed production logs on 15 September 2026 UTC:

- 01:06:42: database migrations applied.
- 01:06:55: accounting projections backfilled for 190 saved pools without RPC.
- 01:07:04: historical writer acquired its lock.
- 01:07:06: a new pool analytics publication succeeded.

At 01:07:21 UTC, production API readiness, Explore, leaderboard and wallet routes
returned HTTP200. The wallet response exposed the new normalized-read contract
(`positionRealizationsIncluded:false`), confirming the new API was serving.
The production leaderboard page then rendered populated rankings: top wallet
`0x474583e46d2ea052fb5690bdebdb41d6cf1ebce1`, realized7771179929655035wei
at that saved cutoff. The page reported236 discovered /192 processed pools.
These counts change, and the cutoffs remained historical, not near-head coverage.

This is a verified deployment checkpoint, not completion of the full product goal.

## One-command read-only sanity check

From the repository root:

```sh
node --env-file-if-exists=.env.local scripts/check-indexed-health.mjs
```

It uses INDEXER_API_URL, calls only the read API, and reports readiness, endpoint
latencies, saved coverage, recent-feed lag, and one top-trader/profile PnL comparison.
It does not query RPC, access Postgres, change configuration, or publish anything.
Exit0 means healthy endpoints and a current recent feed; exit1 means a request or
response validation failed; exit2 means stale/uninitialized recent data or a PnL
comparison that needs rechecking. Separate responses may observe different
publications, so a mismatch alone does not prove an accounting defect.

Verified against production at01:15:55UTC: all5 requests HTTP200,240 discovered,
212 processed,191 qualifying traders, top-wallet PnL matched exactly. Exit2 correctly
reported stale recent activity (14928-block lag,1562seconds old). Whole-market
coverage remains incomplete even when the command eventually exits0.

## Shared-range collection foundation

The sanity-check commit `5242a82` passed full CI run `34916603104`.
A subsequent production sample at 01:23:22 UTC showed 243 discovered pools,
219 processed pools and 192 qualifying traders. The sampled wallet PnL still
matched. Recent lag had grown to 17,432 blocks / 1,803 seconds, so the sampled
worker was not catching up simply by waiting.

`packages/chain/src/pool-event-group.ts` now exports `collectPoolEventGroup`.
It reads one common block range for up to 200 verified, distinct token/pool pairs:
scoped manager swap logs, token transfer logs, shared headers and shared receipts.
It reuses the existing per-pool decoder and evidence checks and rechecks the
common canonical cutoff before returning any results. Out-of-scope logs fail
closed before partitioning. Quiet pools retain complete empty-range boundaries.
The group has a 2,000-block / 10,000-log cap; existing RPC transport budgets apply.
Two logical log queries can still become many HTTP/RPC requests under a provider's
10-block range limit. Each returned `requests` value is the cumulative group RPC
request count, not an additive per-pool count.

This helper is NOT wired into production scheduling or persistence. Production
throughput has not changed from this helper alone. The next implementation must
select common ranges, atomically persist all affected pools with a group cursor,
and handle restart/reorg/rescan provenance before enabling it. Do not loop over
independent `commitBatch` transactions and then advance a global cursor.
This helper neither discovers missing launches nor repairs earlier coverage.

Verification: all 45 chain tests passed, including equality against independent
per-pool collection for a routed transaction, one shared receipt fetch, 200 quiet
pools, unexpected/duplicate evidence, invalid receipts and a changed cutoff.

## Atomic pool-group persistence

Shared collector commit `9bec11e` passed full CI run `34917280215`.
`packages/db/src/index.ts` now also exports `commitPoolGroup(db, entries)` for
up to 200 pool batches sharing the same range and canonical cutoff. It commits
all evidence, events and per-pool cursors in one transaction, or rolls back all
members. It reuses the existing content-hash replay, token-identity, continuity
and event validation. Stream locks use a deterministic order; returned replay
flags retain input order. The existing single-batch API keeps its transaction
contract through the same internal persistence function.

This requires no new table or migration. It is not a global discovery cursor and
is not yet called by the production scheduler. The next worker change needs to
select pools with a common contiguous range (existing pool starts/cursors often
differ), reconcile each checkpoint, collect once, and commit with this group API.
Do not skip a pool's earlier unprocessed blocks to force range alignment.
Versioned discovery/rescan provenance remains a separate unresolved requirement.

Local Postgres checks cover a later member failing after an earlier member wrote,
whole-group rollback, restart/replay, stale-member rejection, empty groups,
duplicate streams and canonical replacement after rewind. No production database
mutation was performed to validate these cases.

## Worker integration supersedes the unwired-helper notes above

Atomic persistence commit `4f32396` passed full CI run `34917774486`.
The historical worker now selects the oldest-attempted pool plus peers with the
same next unprocessed block, within the existing INDEXER_POOLS_PER_CYCLE budget.
It uses the shared collector and atomic commit for groups larger than one.
Single-pool ranges end at a shared batch-size boundary: a stream starting at12
with a10-block batch processes12-19, then20-29. No earlier blocks are skipped or
claimed covered. Existing streams gradually become compatible for grouping.

Checkpoint reconciliation moved unchanged into `apps/indexer/src/checkpoints.ts`.
The grouped worker shares starting header observations, then performs fresh
boundary checks before publishing. If reconciliation changes group alignment, or
collection/commit fails, the scheduler re-reads and retries members individually.
A bad member therefore does not indefinitely block its healthy peers. Discovery
continues independently. The existing provider pacing, two-pool cycle setting,
writer locks and deployment configuration remain unchanged.

Look for `indexed_group` logs with pool count, range, swap/transfer counts,
HTTP/RPC counts and elapsed time. `pool_group_fallback` explains a group retry.
Grouping only helps pools whose cursors align; this is not yet the global
historical discovery/rescan implementation and does not establish complete
market coverage or prove the separate recent feed has caught up.

Local validation passed: all29 database checks, workspace types, unit tests and
lint. The new worker integration test uses real local Postgres and controlled RPC
evidence to cover distinct launch starts, contiguous alignment, shared receipts,
swaps and token transfers, invalid receipt rejection, restart and canonical
replacement after a simulated reorg. Full CI and production rollout for this
worker integration still need checking before calling this iteration deployed.

## Verified worker rollout

Worker integration commit `5e4dbeb` passed full CI run `34918576626`.
Railway deployment `8f5ac26a-85bc-46c4-8991-ece20c1b28f6` became Active.
Observed startup on 15 September 2026 UTC: migrations at01:53:34,
writer lock acquired at01:53:59, then a discovery batch at01:55:09
(blocks62806935-62807934, one launch,63HTTP requests /115RPC calls).
The previous worker's lock was respected during the rolling deployment.

Production API checks at01:54:48UTC all returned HTTP200 in137-237ms in that
single sample. Catalog252, processed229, qualifying traders192. Sampled top
wallet realized PnL still matched7771179929655035wei exactly. The recent feed
remained stale:21591blocks /2248seconds at that observation.

At01:55:12UTC the recent worker completed250blocks in73052ms (74HTTP requests,
138RPC calls), with60 accepted swaps out of901 observed swaps;841 were
unregistered. Its saved cutoff was63261915 against observed head63283256.
This is concrete throughput/coverage evidence for the next iteration. The
historical grouping change does not modify the recent worker's one-second
request pacing, its accounting-free swap path, or the missing launch registry
coverage in the database. Do not claim the live feed is fixed. A production
`indexed_group` completion had not yet been observed when this note was written;
the grouped execution path is verified by the local Postgres integration test.

## Confirmed Alchemy limits and pending manual choice

Read the authenticated Alchemy Usage page on15September2026. Eddy's Team has
one app and an actual throughput limit of300CU/s. The seven-day peak was467.8CU/s;
the dashboard displayed a throughput-exceeded warning. Monthly use was6.3M of
30M CUs, with23.7M remaining. The peak is historical, not proof of a current429.
No billing, alert, quota or account setting was changed.

The public pricing page advertises500CU/s for Free, while the docs say300. The
account's displayed300CU/s is authoritative for this project. Throughput is
account-wide, so the three supervised workers share it. Independent one-second
HTTP pacing is not a shared compute-unit budget and also delays cheap receipt
and header reads unnecessarily.

For one contiguous launch-log scan from earliest verified deployment22754669 to
observed head63283256, the range is40528588blocks. At10blocks per log request this
requires4052859requests. At the documented60CU per eth_getLogs, that is243171540CU,
about8.1 free monthly allowances, before receipts, metadata, swaps or retries.
This is a calculation for this RPC scanning method, not a count of tokens or a
complete cost estimate. PAYG supports wider Robinhood Mainnet log queries.

An explicit user question is pending: upgrade the existing Alchemy account to
PAYG, or keep Free. Recommend the existing-provider upgrade for full historical
coverage. It does not require adding another provider; verify the existing
endpoint's new limits before tuning batch ranges/rates. Do not upgrade billing
without the user's decision. If staying Free, shared request pacing/priorities
can improve the recent feed, but cannot remove the10-block scan allowance cost.

Sources checked:

- https://dashboard.alchemy.com/usage (authenticated account evidence)
- https://www.alchemy.com/docs/reference/throughput
- https://www.alchemy.com/docs/reference/compute-unit-costs
- https://www.alchemy.com/docs/chains/ethereum/ethereum-api-endpoints/eth-get-logs
- https://www.alchemy.com/docs/reference/pricing-plans
- https://www.alchemy.com/pricing

## Ready-to-run endpoint check and pacing configuration

`node --env-file-if-exists=.env.local --import tsx scripts/check-rpc-range.ts`
checks chain4663 and sends one explicit1,000-block query against the verified
strategy addresses. It deliberately bypasses automatic range splitting. Exit0
means that range was accepted; exit2 reports the provider's smaller advertised
limit; exit1 is a request/validation failure. It prints no URL/key and writes no
data. It does not establish the account plan, throughput or full launch coverage.
Verified against the current endpoint: exit2, advertisedBlockLimit10.

The three worker entrypoints now read shared optional `RPC_MIN_INTERVAL_MS` and
`RPC_MAX_BATCH_SIZE` settings through `rpc-pacing.ts`. Defaults remain1000ms and2,
so this preparation does not change current production request speed. Empty or
malformed overrides fail validation. Each child applies the settings separately;
this is still not an account-wide compute-unit allocator.

After the user upgrades and the authenticated account shows the expected paid
capacity, first rerun the range check. A starting configuration to validate is
INDEXER_LOG_RANGE_BLOCKS=1000, RECENT_LOG_RANGE_BLOCKS=1000,
RPC_MIN_INTERVAL_MS=250, RPC_MAX_BATCH_SIZE=10. Do not apply these on the current
300CU/s account. Confirm actual throughput and429 behavior with all children
running, then verify the recent cursor gains on the head. Keep the saved discovery
start unchanged; expanding deployment coverage still needs versioned rescanning.

## Final checkpoint: recent worker batch stability

Previous commit `065b9a1` passed full CI run34919727689. Its new pacing controls
keep existing defaults; no paid plan, provider setting or Railway pacing override
was applied. The user's Alchemy upgrade choice remains unanswered.

Production logs from deployment8f5ac26a-85bc-46c4-8991-ece20c1b28f6 reproduced
unhelpful growth: five250-block successes took54039,70049,67041,67045,68045ms,
then the500-block batch took109055ms against a120000ms collection budget.
The recent worker now grows only after five consecutive full requested batches
with at most45000ms elapsed and100HTTP requests. Partial, idle or slower batches
reset that streak. Success logs include requestedBatchBlocks and nextBatchBlocks.

After acquiring its writer lock, the worker resumes with the width of the saved
swap batch whose block and hash match the cursor, capped by configured maximum.
Absent matching evidence falls back to the configured size. This is only a sizing
hint; normal canonical reconciliation remains mandatory and no cursor is changed.
No schema migration, billing change or request-rate increase is involved.

Local validation: all30 database integration tests passed, including checkpoint
matching, rewind fallback and restart sizing; unit suites passed with no failures
(existing DB-dependent skips covered by the database run); all typechecks passed.
New controller regressions cover the observed production durations, consecutive
fast successes, partial work and request/time limits. Implementation commit
`63fa0b4` is pushed to main and full CI run34920479033 passed, including both
containers and browser tests. The Railway rollout of this commit is not yet
verified at that checkpoint; the subsequent verification below supersedes this.

This does not establish complete launch history, complete wallet PnL, or a caught-up
recent feed. Remaining priorities are the paid RPC decision/range verification,
versioned historical launch rescanning, and evidence-backed full-market accounting.
Preserve this checkpoint for transfer; do not open another architecture iteration
just to consume the remaining account quota.

## Final rollout verification, 2026-09-15 02:24 UTC

Railway indexer deployment `2a604652-2027-4ba6-915f-d6994e22ee69`, title
"Keep recent batch growth within measured headroom", is Active. Its logs show
migrations applied, writer acquired, and completed recent batches after restart:

- 02:23:35 UTC: requested250, advanced250, next250, through63267665,
  elapsed42066ms, HTTP43/RPC72, swaps16, observed8664/unregistered8648.
- 02:23:58 UTC: requested250, advanced250, next250, through63267915,
  elapsed28031ms, HTTP29/RPC44, swaps2, observed4243/unregistered4241.
- Discovery completed62814935..62815934 and historical pool indexing also resumed.

This proves startup and persisted progress, including the restart sizing hint.
It does not prove sustained catch-up: the second batch's lag was32742 blocks.
Observed Uniswap manager swaps outside the verified registry remain excluded;
these counts alone do not distinguish missing Pools launches from unrelated pools.

Read-only API sanity check at02:23:23UTC returned five HTTP200 responses,
136-282ms: readiness, explore, leaderboard, live trades and the top wallet.
Catalog257, processed232, qualifying traders192. The top wallet's leaderboard
and profile agree at7771179929655035wei realized. Recent state was stale,
32829blocks/3373seconds behind, so the checker correctly exited2, not0.
This is a deployment checkpoint, not completion of the product goal. No billing,
RPC pacing override, database networking or cursor reset was performed.

## ENS search integration follow-up

Reproduced a browser-level gap: `creator:indexed.eth` resolved to an address but
never queried saved search, leaving a generic unverified-creator link even when
the creator existed in the saved catalog. The search provider now carries the
resolved ENS address into the normal asynchronous index extension, preserving
typed categories and defaulting unqualified names to wallets. Existing fallback
results stay usable while the index loads or fails. No new resolver/provider/key
is added, and unresolved names do not trigger a fabricated address search.

Validated before/after with an intercepted ENS response and a saved-only creator
fixture in the real browser UI. Eight targeted desktop/mobile E2Es passed,
including slow/offline-index wallet fallback, profile navigation, existing typed
search and saved-only token results. Production build, web typecheck and lint
passed. Implementation commit `4807ca4` subsequently passed full CI run34921297796,
including the full browser suite. Vercel reported successful deployment
`FHF33qgXrdQLmkrWo7WvAL6yp7rm`; the indexer correctly skipped this web-only change.

Verified in the production Ctrl-K UI with `creator:vitalik.eth`: real ENS request
returned200, followed by saved search for
`creator:0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045` returning200 after the normal
trailing-slash redirect. The UI displayed saved-index coverage and an unverified
creator fallback, correctly making no claim of launch activity for that address.
The saved-only verified creator case is covered by the controlled browser fixture,
not by this production address. No new provider credentials or service was added.

## Historical pool budget retry follow-up

Production observation on2026-09-15: at02:35:30UTC deployment
2a604652-2027-4ba6-915f-d6994e22ee69 logged a collection-budget failure for pool
0xb3fa833db1536bb9cd8c76644256e3794010c71b20a8c772cb98fa639c8e54a6,
HTTP122/RPC291. The historical scheduler retried pools at a fixed configured size,
unlike the recent worker's adaptive batches. This could repeatedly prevent a dense
pool from advancing even though smaller ranges fit the existing provider budget.

`PoolBatchBudget` now wraps singleton and grouped historical pool attempts. Only
recognized collection time/request/log-count limits halve the range, down to one
block; every attempt re-reads durable cursors and uses fresh canonical validation.
Other errors propagate unchanged. The process retains reduced hints by pool key;
unrelated pools keep their normal size. `pool_batch_reduced` logs old/new sizes.
Hints reset on process restart and do not automatically grow. This is intentionally
separate from durable coverage and does not change RPC pacing or configuration.

Local validation passed: all30 real Postgres tests, all typechecks, unit suites with
no failures. The group worker integration test injects the observed budget error,
proves both cursors stay unchanged, then advances two contiguous smaller ranges
without duplicating events. Unit tests cover hint isolation, bounded retries,
cancellation and non-budget failures. Implementation `e243ba9` passed full CI
run34922023309, including container and browser checks. Railway deployment
`d55065be-ee91-4cbf-8b10-5d2bd6fdda5f` is Active and acquired its writer lock.
At02:47:36UTC it discovered62821935..62822934; at02:48:16UTC it indexed
pool0x76090448b43fef6b6626f7454538244e608f20ffe20e925dded41a5440e2b8eb,
blocks62696751..62696999, with one swap and one transfer. A recent500-block batch
also completed. This proves rollout and resumed work; an actual production
`pool_batch_reduced` event has not yet been observed. Reduction behavior is proven
by the controlled integration test, not by those normal-size production successes.

The post-deploy API check at02:46:35UTC returned five HTTP200 responses,
127-276ms: catalog267, processed236, qualifying traders192, and the same exact
top-wallet/profile PnL match. Recent lag33675blocks/3469seconds remained stale.
No database schema, provider limits, billing or network settings changed.

The read-only API snapshot at02:36:35UTC was healthy: catalog262, processed235,
qualifying traders192, matching top-wallet realized PnL. Recent lag32380blocks,
age3327seconds. Logs showed recent growth to1000-block successes in65-73seconds,
with modest net catch-up, not a current feed. Historical rescanning remains unbuilt.
